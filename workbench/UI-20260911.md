# 工作台界面更新 · 2026-09-11

> 历史记录：本文描述 2026-09-10 至 2026-09-11 的部署与验收。独立网页已于 2026-09-13 卸载；当前入口、配置与维护方式见 [工作台说明](README.md)。

已部署到 http://10.16.9.237:3100 ，刷新页面即可使用。

- 输入框工具栏提供技能、插件、知识库多选浮层，支持搜索和键盘操作，选择自动保存到当前会话。
- 失效资源自动移除并保存；新增资源保留为未勾选状态。插件优先显示 Dify 声明的中文名称。
- Enter 发送，Shift+Enter 换行；中文输入法选字不会触发发送。
- 执行记录按文字、工具调用的实际顺序展示。工具输入和结果可以展开，重复事件合并，最终回复只显示一次。
- 页面随窗口撑满，聊天记录和列表在内部滚动。文件空间入口位于右上角。
- 界面已简化工作区、会话删除、模型参数和配置修订信息。

验证：前端类型检查、lint、Linux 构建通过；后端 18 项针对性测试及 Ruff 通过。浏览器验证了自动清理、连续勾选后保存、切换会话恢复、搜索、方向键、Enter、Esc、多行输入与输入法保护。

生产页面实测 1920×1080、1440×900、1280×720、1024×600、768×600、390×844、320×568、844×390，页面级横向和纵向溢出均为零。实际生产 `qwen3.8-flash` 通过 Enter 发送后调用 Shell，返回 `WB_UI_20260911_OK`；验收会话已清理。

API / API WebSocket 使用 `dify-workbench-api:1.17.0-20260911.1`，前端使用 `dify-workbench-web:33085b66-20260911.1`。本次更新沿用现有执行器、Worker、会话令牌和数据库结构。镜像 ID 和部署前配置备份见 `ui-update-verification.json`。
